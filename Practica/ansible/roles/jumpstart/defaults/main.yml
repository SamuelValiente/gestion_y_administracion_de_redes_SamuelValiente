# Interfaces y redes del NAT temporal de aprovisionamiento

jumpstart_internet_iface: eth0 # interfaz hacia el gateway externo, ajustar en GNS3 el que se encargue
jumpstart_main_subnet: 192.168.1.0/24
jumpstart_internal_subnet: 10.0.0.0/24
